# Generated from build-manifests/vc6-projects.json for the original EA VC6 project "wwbitpack".
# Keep this source membership descriptive until baseline parity is established.

ea_vc6_static_library(wwbitpack
  SOURCES
    BitPacker.cpp
    BitPacker.h
    bitpackids.h
    bitstream.cpp
    bitstream.h
    encoderlist.cpp
    encoderlist.h
    encodertypeentry.cpp
    encodertypeentry.h
  INCLUDE_DIRS
    ../wwdebug
    ../wwlib
    ../wwutil
)
